# References

Put reusable rules, examples, source summaries, and domain references here.

Exported packages copy this directory to `skills/_shared/references/` so the target agent can reuse known context before recreating it.
