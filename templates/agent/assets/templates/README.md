# Templates

Put reusable output templates, document shells, prompt templates, or other stable artifacts here.

Exported packages copy this directory to `skills/_shared/templates/` so the target agent can reuse existing output formats before creating new ones.
